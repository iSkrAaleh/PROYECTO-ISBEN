from django import forms
from django.contrib.auth.forms import AuthenticationForm

# Usamos el AuthenticationForm de Django para el login, tal como se aprendió en clase.
# Los estilos se aplican desde login.css capturando input[type="text"] e input[type="password"]
